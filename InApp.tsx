import {View} from 'react-native';
import ReactMoE, {
  MoEInitConfig,
  MoEPushConfig,
  MoEngageLogConfig,
  MoEngageLogLevel,
  MoEProperties,
} from 'react-native-moengage';

const InApp = ({route}) => {
  //const data = route.params.data;
  //console.log('Theeee data', data);

  return <View></View>;
};

export default InApp;
